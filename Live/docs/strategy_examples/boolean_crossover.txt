# Boolean Crossover

fast = ta.ema(close, 9)
slow = ta.ema(close, 21)

bullCross = ta.crossover(fast, slow)
bearCross = ta.crossunder(fast, slow)

plot fast
plot slow

buy when bullCross
sell when bearCross
