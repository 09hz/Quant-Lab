# Patch 34h — Watch daily replay render fix\n\nThis patch fixes a higher-timeframe Watch chart issue where daily candles could appear missing after switching from intraday replay views.\n\n## Changes\n\n- Makes `1 hour` replay default to the latest session-style `1D` viewport.\n- Makes `1 day` replay default to `MAX` so daily candles are visible immediately.\n- Clears stale intraday manual zoom state when switching/loading daily replay.\n- Adds the selected display timeframe to Watch chart `uirevision` / `datarevision` so Plotly does not preserve a stale lower-timeframe viewport across timeframe changes.\n\n## Files\n\n- `Live/services/watch_chart_state.py`\n- `Live/callbacks.py`\n